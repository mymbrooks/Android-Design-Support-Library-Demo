<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.ruibu.m">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Android Design Support Library">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TextInputLayoutDemoActivity"
            android:label="TextInputLayoutDemo" />
        <activity
            android:name=".FloatingActionButtonDemoActivity"
            android:label="FloatingActionButtonDemo" />
        <activity
            android:name=".SnackbarDemoActivity"
            android:label="SnackbarDemo" />
    </application>

</manifest>